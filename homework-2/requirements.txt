# ── Core web framework ─────────────────────────────────────────────────────────
fastapi>=0.111.0            # Async web framework with built-in OpenAPI support
uvicorn[standard]>=0.29.0   # ASGI server (includes websockets + httptools)

# ── Data validation ────────────────────────────────────────────────────────────
pydantic[email]>=2.7.0      # Data models + validators (email extra adds EmailStr)

# ── File upload support ────────────────────────────────────────────────────────
python-multipart>=0.0.9     # Required for FastAPI UploadFile / multipart forms

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.2.0               # Test runner
pytest-cov>=5.0.0           # Coverage plugin for pytest
httpx>=0.27.0               # Async HTTP client (required by FastAPI TestClient)
pytest-asyncio>=0.23.0      # Async test support (for concurrent tests)
